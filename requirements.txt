torch>=2.2
datasets>=2.18
tokenizers>=0.15
lxml>=5.0
cairosvg>=2.7         # primary renderer (Linux/macOS, needs libcairo2)
resvg-py>=0.1         # Windows-friendly renderer, pure Rust, no system deps
svglib>=1.5           # last-resort renderer; reportlab 4.x also needs libcairo
reportlab>=4.0
mup==1.0.0
pyyaml
numpy
scipy
matplotlib
pandas
pillow                # placeholder PNGs + image grid
jupyter
nbconvert
tqdm

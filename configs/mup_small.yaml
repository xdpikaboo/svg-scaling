extends: base.yaml
name: mup_small
parameterization: mup

model:
  n_layer: 4
  n_head: 4
  d_model: 192
  d_ff: 768                 # 4 x d_model

batch:
  micro_batch: 64           # 64 x 1024 x 8 grad_accum = 524288
